import 'package:flutter/material.dart';

class AddSlackChannelScreen extends StatelessWidget {
  const AddSlackChannelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column()),
    );
  }
}
