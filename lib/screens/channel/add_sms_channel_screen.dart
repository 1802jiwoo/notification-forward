import 'package:flutter/material.dart';

class AddSmsChannelScreen extends StatelessWidget {
  const AddSmsChannelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Column()),
    );
  }
}
